# Featurable — free Google Reviews feed (no Google Cloud billing).
# Copy this file to ".env" and fill in the real value (never commit .env).
# Get the widget ID from your Featurable dashboard → widget → API tab.
FEATURABLE_WIDGET_ID=your-featurable-widget-id
